baseURL = "https://blog.acmattheis.de/"
languageCode = "en-us"
title = "Testing Hugo and netlify-CMS Site"

# Change the default theme to be use when building the site with Hugo
theme = "hugo-theme-learn"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]
